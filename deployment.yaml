apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-calculator
  # namespace: namespace-calculator:1.0.0-17
  labels:
    app: calculator-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: calculator-app
  template:
    metadata:
      labels:
        app: calculator-app
    spec:
      containers:
      - name: c_calculator
        image: raemondarellano/calculator:1.0.0-22
        ports:
        - containerPort: 80
